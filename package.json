{
  "name": "@ferdousbhai/slop-detector",
  "version": "1.1.1",
  "private": true,
  "description": "Deterministic linting for AI-flavored prose in browsers, repositories, and agent output",
  "packageManager": "npm@12.0.2",
  "license": "MIT",
  "type": "commonjs",
  "engines": {
    "node": ">=26",
    "npm": ">=12"
  },
  "scripts": {
    "check": "node --check bin/slop-detector.js && node --check lib/linter.js && node --check extension/background.js && node --check extension/engine.js && node --check extension/popup.js && node --check extension/scanner.js && npm run check:versions",
    "check:versions": "node scripts/check-versions.js",
    "test": "node --test test.js test-scanner.js test-extension.js test-cli.js",
    "package:chrome": "bash scripts/package-chrome.sh"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
